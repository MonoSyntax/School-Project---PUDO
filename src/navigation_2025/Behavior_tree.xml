<root BTCPP_format="4" main_tree_to_execute="MainTree">
  
  <!-- ================================================================== -->
  <!-- MAIN NAVIGATION TREE -->
  <!-- ================================================================== -->
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="10" name="NavigateRecovery">
      
      <PipelineSequence name="NavigateWithReplanning">
        
        <!-- Algorithm Selectors -->
        <ProgressCheckerSelector 
          selected_progress_checker="{selected_progress_checker}" 
          default_progress_checker="progress_checker" 
          topic_name="progress_checker_selector"/>
        
        <GoalCheckerSelector 
          selected_goal_checker="{selected_goal_checker}" 
          default_goal_checker="general_goal_checker" 
          topic_name="goal_checker_selector"/>
        
        <ControllerSelector 
          selected_controller="{selected_controller}" 
          default_controller="FollowPath" 
          topic_name="controller_selector"/>
        
        <PlannerSelector 
          selected_planner="{selected_planner}" 
          default_planner="GridBased" 
          topic_name="planner_selector"/>
        
        <!-- Path Planning at 1 Hz -->
        <RateController hz="1.0">
          <RecoveryNode number_of_retries="3" name="ComputePathThroughPoses">
            
            <ReactiveSequence>
              <RemovePassedGoals 
                input_goals="{goals}" 
                output_goals="{goals}" 
                radius="1.0" 
                input_waypoint_statuses="{waypoint_statuses}" 
                output_waypoint_statuses="{waypoint_statuses}"/>
              
              <ComputePathThroughPoses 
                goals="{goals}" 
                path="{path}" 
                planner_id="{selected_planner}" 
                error_code_id="{compute_path_error_code}" 
                error_msg="{compute_path_error_msg}"/>
            </ReactiveSequence>
            
            <Sequence>
              <WouldAPlannerRecoveryHelp error_code="{compute_path_error_code}"/>
              <ClearEntireCostmap 
                name="ClearGlobalCostmap-Context" 
                service_name="global_costmap/clear_entirely_global_costmap"/>
            </Sequence>
            
          </RecoveryNode>
        </RateController>
        
        <!-- Traffic-Aware Path Following -->
        <ReactiveFallback name="TrafficAwareFollowing">
          
          <!-- Priority 1: Traffic Light Compliance -->
          <SubTree ID="TrafficLightHandler"/>
          
          <!-- Priority 2: Stop Sign Compliance -->
          <SubTree ID="StopSignHandler"/>
          
          <!-- Priority 3: Normal Path Following -->
          <RecoveryNode number_of_retries="3" name="FollowPath">
            
            <FollowPath 
              path="{path}" 
              controller_id="{selected_controller}" 
              error_code_id="{follow_path_error_code}" 
              error_msg="{follow_path_error_msg}" 
              goal_checker_id="{selected_goal_checker}" 
              progress_checker_id="{selected_progress_checker}"/>
            
            <Sequence>
              <WouldAControllerRecoveryHelp error_code="{follow_path_error_code}"/>
              <ClearEntireCostmap 
                name="ClearLocalCostmap-Context" 
                service_name="local_costmap/clear_entirely_local_costmap"/>
            </Sequence>
            
          </RecoveryNode>
          
        </ReactiveFallback>
        
      </PipelineSequence>
      
      <!-- Recovery Actions -->
      <Sequence name="RecoverySequence">
        
        <Fallback>
          <WouldAControllerRecoveryHelp error_code="{follow_path_error_code}"/>
          <WouldAPlannerRecoveryHelp error_code="{compute_path_error_code}"/>
        </Fallback>
        
        <ReactiveFallback name="RecoveryFallback">
          <GoalUpdated/>
          
          <Sequence name="SimpleRecovery">
            <ClearEntireCostmap service_name="local_costmap/clear_entirely_local_costmap"/>
            <ClearEntireCostmap service_name="global_costmap/clear_entirely_global_costmap"/>
            <Wait wait_duration="1.0"/>
            
            <Fallback>
              <Sequence>
                <IsSafeToBackup distance="0.5"/>
                <BackUp backup_dist="0.5" backup_speed="0.2" time_allowance="15.0"/>
              </Sequence>
              <AlwaysSuccess/>
            </Fallback>
            
            <ComputePathThroughPoses goals="{goals}" path="{path}" planner_id="{selected_planner}"/>
          </Sequence>
          
        </ReactiveFallback>
        
      </Sequence>
      
    </RecoveryNode>
  </BehaviorTree>
  
  
  <!-- ================================================================== -->
  <!-- TRAFFIC LIGHT HANDLER -->
  <!-- ================================================================== -->
  <BehaviorTree ID="TrafficLightHandler">
    <ReactiveFallback name="HandleTrafficLight">
      
      <!-- Check 1: No relevant red/yellow light → Proceed -->
      <Inverter>
        <Fallback>
          <!-- Red light in detection zone (3-15m) -->
          <IsRedLightDetected 
            min_distance="3.0"
            max_distance="15.0"/>
          
          <!-- Yellow light in detection zone (3-15m) -->
          <IsYellowLightDetected 
            min_distance="3.0"
            max_distance="15.0"/>
        </Fallback>
      </Inverter>
      
      <!-- Check 2: Red/Yellow detected → Stop and wait 10 seconds -->
      <Sequence name="StopForTrafficLight">
        
        <!-- Stop the robot -->
        <PublishZeroVelocity duration="0.5"/>
        
        <!-- Wait 10 seconds (since light changing is broken) -->
        <Wait wait_duration="10.0"/>
        
      </Sequence>
      
    </ReactiveFallback>
  </BehaviorTree>
  
  
  <!-- ================================================================== -->
  <!-- STOP SIGN HANDLER -->
  <!-- ================================================================== -->
  <BehaviorTree ID="StopSignHandler">
    <ReactiveFallback name="HandleStopSign">
      
      <!-- Check 1: No relevant stop sign → Proceed -->
      <Inverter>
        <IsStopSignDetected 
          min_distance="3.0"
          max_distance="15.0"/>
      </Inverter>
      
      <!-- Check 2: Stop sign detected → Stop and wait 10 seconds -->
      <Sequence name="StopForStopSign">
        
        <!-- Stop the robot -->
        <PublishZeroVelocity duration="0.5"/>
        
        <!-- Wait 5 seconds -->
        <Wait wait_duration="5.0"/>
        
      </Sequence>
      
    </ReactiveFallback>
  </BehaviorTree>
  
  
  <!-- ================================================================== -->
  <!-- ROBOT STOPPING BEHAVIOR (NOT NEEDED - PublishZeroVelocity is inline) -->
  <!-- ================================================================== -->
  
</root>